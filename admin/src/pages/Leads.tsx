// Этап M4: таблица лидов (профили из `conversations.lead`) и записей на
// экскурсии (`bookings`) со ссылками на события в Google Calendar.
export function Leads() {
  return (
    <section>
      <h1>Лиды и записи</h1>
      <p className="stub">Страница появится на этапе M4.</p>
    </section>
  );
}
