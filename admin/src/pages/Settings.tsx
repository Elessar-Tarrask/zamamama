// Этап M4: форма редактирования документа Firestore `settings/bot` —
// системный промпт, приветствие, питчи по возрастам, fallback-тексты,
// глобальный вкл/выкл бота, часы экскурсий и длина слота, ID календаря,
// номер администратора для алертов.
export function Settings() {
  return (
    <section>
      <h1>Настройки бота</h1>
      <p className="stub">
        Страница появится на этапе M4. Данные: документ <code>settings/bot</code>.
        Изменения применяются со следующего сообщения — деплой не нужен.
      </p>
    </section>
  );
}
