// Этап M4: список диалогов (`conversations`) с бейджами режима бот/человек,
// транскрипт (`conversations/{phone}/messages`), кнопки «забрать диалог» /
// «вернуть боту» (callable adminSetMode) и ручная отправка сообщения
// (callable adminSendMessage — ставит бота на паузу в этом чате).
export function Conversations() {
  return (
    <section>
      <h1>Диалоги</h1>
      <p className="stub">Страница появится на этапе M4.</p>
    </section>
  );
}
